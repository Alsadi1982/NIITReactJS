const initialState = [ { id: 0, name: 'Карандаш'}];

function orderList (state = initialState, action) {
    switch (action.type) {
        default:
            return state;

    }
}

export { orderList }